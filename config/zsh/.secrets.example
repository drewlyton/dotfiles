# Copy this file to .secrets and fill in your real values:
#   cp .secrets.example .secrets
#
# These are sourced by .zshrc and used by opencode.json via {env:VAR} interpolation.
# Never commit .secrets to version control.

export SANITY_MCP_TOKEN=""
export GROWTHBOOK_API_KEY=""
export LINEAR_API_TOKEN=""
